# `PoolCursor`

Wrapper do cursor SQLite — expõe métodos como atributos.

## Métodos e propriedades

| Acesso | O que faz |
|---|---|
| `.close()` |  |
| `.execute(sql, params=None)` |  |
| `.executemany(sql, seq)` |  |
| `.fetchall()` |  |
| `.fetchmany(size=1)` |  |
| `.fetchone()` |  |
| `.lastrowid` |  |
| `.rowcount` |  |

[← índice](../sqlite3.md)
