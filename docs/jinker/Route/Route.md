<!-- gerado: gera_doc_gaps.py — pode regenerar -->
# `Route`

> **Objeto interno da linguagem** — você não cria `Route` na mão:
> é o TIPO de um objeto que a lib `jinker` te entrega pronto.
> Confira com `type(obj)`, que mostra exatamente este nome.

Uma rota registrada no Jinker.

## Métodos e propriedades

| Acesso | O que faz |
|---|---|
| `.auth` | atributo |
| `.handler` | atributo |
| `.methods` | atributo |
| `.middleware` | atributo |
| `.path` | atributo |

[← índice](../jinker.md)
