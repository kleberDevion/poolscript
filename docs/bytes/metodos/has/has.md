# `b.has(sub)`

O mesmo que `contains` — as duas grafias existem na linguagem.

## Parâmetros

| nome | default |
|---|---|
| `sub` | — |

## Retorno

bool

## Exemplos

```ps
import bytes
post("Hello".encode().has("He".encode()))
```

```saida
True
```

[← índice](../../bytes.md)
