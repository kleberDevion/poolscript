# `.text(valor)`

Define o **texto** do elemento (str ou número). Encadeável. Sem `.text()`, o
elemento mostra o `placeholder=` (se tiver).

```
app.p().text("Bem-vindo")
app.entry(placeholder="seu nome")     // sem .text(): mostra a dica
```

[← índice](../guzer.md)
