# `window()`

A **janela** do app: tamanho e cor de fundo. Sem `window()`, o default é
**480×320** com fundo branco.

```
app.window().stylesheet({ "width": "500", "height": "300", "background": "#101418" })
```

`type()` devolve `"Window"`.

[← índice](../guzer.md)
